language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t goldentycho/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true goldentycho/docker-react npm run test -- --coverage